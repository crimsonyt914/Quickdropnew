{
  "name": "quickdrop",
  "private": true,
  "version": "1.0.0",
  "description": "QuickDrop — send files. Simple. Fast.",
  "main": "apps/desktop/dist-electron/main.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/server & npm run dev --workspace=apps/desktop",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:desktop": "npm run dev --workspace=apps/desktop",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/server && npm run build --workspace=apps/desktop",
    "dist": "npm run build && npm run dist --workspace=apps/desktop",
    "postinstall": "npm run build --workspace=packages/shared"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
